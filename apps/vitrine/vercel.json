{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "PRERENDER_API_URL=https://api.socialveiculos.com.br pnpm build",
  "outputDirectory": "dist",
  "routes": [
    { "src": "/v1/(.*)", "dest": "https://api.socialveiculos.com.br/v1/$1" },
    { "src": "/static/(.*)", "dest": "https://api.socialveiculos.com.br/static/$1" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
