{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm build",
  "outputDirectory": "dist",
  "routes": [
    { "src": "/v1/(.*)", "dest": "https://api.socialveiculos.com.br/v1/$1" },
    { "src": "/static/(.*)", "dest": "https://api.socialveiculos.com.br/static/$1" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
